#include<stdio.h>

int main()
{
	   int num1, num2;
	    
		printf("Digite dois numeros:");
		scanf("%d %d", &num1, &num2);
		
		printf(" Condicao: %s\n", (num1%2 == 0 && num2%2 == 0) ? "Ambos numeros pares" : "Um numero nao par");
		
		return 0;	
}
